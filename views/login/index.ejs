<!DOCTYPE html>
<html>
    <head>
        <!--Let browser know website is optimized for mobile-->
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        
        <% include ../common/css.ejs %>

        <!-- start of your own css here -->
        <link type="text/css" rel="stylesheet" href="/css/login.css"/>
        <!-- end of your own css        -->

    </head>
    <body class="teal lighten-1">
            <nav>
                <div class="nav-wrapper teal lighten-1">
                  <a href="#" class="brand-logo">Reseta</a>
                </div>
            </nav>
            
            <div class="container">
                
                <!-- <center>
                    <div class="logo-padding">
                        <img class="responsive-img" src="/images/logo.png" width="150" height="150">
                    </div>
                </center>

                <div class="row">
                    <div class="sm6">
                        <input type="text">
                    </div>
                    <div class="sm6">
                        <input type="text">
                    </div>
                    <div class="sm6">
                        <a href="#!" class="btn">Login</a>
                    </div>    
                </div> -->

                <ul class="collection">
                    <% for(let i=0;i<patient.length;i++){ %>
                        <% let elem = patient[i]; %>
                        <li id="<%= elem.id %>" class="collection-item" data-info="<%= JSON.stringify(elem) %>"><%= elem.name %></li>
                    <% } %>
                </ul>

            </div>


            <% include ../common/js.ejs %>
    <script src="/js/login.js"></script>
    </body>
</html>